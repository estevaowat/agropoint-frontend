import React from 'react';

import './styles.css';

const Header: React.FC = () => (
  <header>
    <strong>Agropoint </strong>
    <p>
      Importe um arquivo csv para verificar os pontos no mapa ou carregue os já
      existentes
    </p>
  </header>
);

export default Header;
